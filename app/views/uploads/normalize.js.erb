 var notice = $('#notice');
 if ( notice.text().length > 0 ) {
     notice.html("Files were successfully uploaded!!").delay(1500).fadeOut('slow'); 
    }

 $("div.multiselect new-navigation-alternate round").hide().after(<%= j render "normalize" %>)

 
