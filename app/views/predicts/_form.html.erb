<%= form_for(@predict) do |f| %>
  <% if @predict.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@predict.errors.count, "error") %> prohibited this predict from being saved:</h2>

      <ul>
      <% @predict.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
